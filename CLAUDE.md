# 구구단성의 보물 — 프로젝트 안내

9살 아이를 위한 구구단 학습 게임. 순수 HTML/CSS/JS(빌드 도구 없음).
GitHub Pages로 배포: https://daeeunbae-droid.github.io/gugudan-castle/

## 중요 — 작업 전 반드시 읽을 것

- **`img/` 폴더의 파일은 절대 읽지 말 것.** PNG/JPG 바이너리라 컨텍스트만 날아간다.
  이미지 목록이 필요하면 파일명만 확인한다.
- 코드는 전부 `index.html` + `css/style.css` + `js/*.js` 안에 있다. 이 파일들만 읽으면 된다.
- ES 모듈을 쓰지 않는다. 일반 `<script>` 태그로 순서대로 로드된다.
  전역 변수를 서로 공유하므로 **로드 순서를 바꾸면 안 된다.**
- `js/` 파일들은 서로 참조한다. 하나만 고치고 끝내지 말고,
  참조하는 다른 파일도 함께 확인할 것.

## 파일 구조

```
index.html        화면 구조 (section 단위)
css/style.css     전체 스타일
js/data.js        ★ 몬스터·아이템·상점·난이도·보상 — 밸런스는 여기만 고침
js/sync.js        기기 간 이어하기 (Firebase, 기본 비활성)
js/save.js        localStorage 저장 · 버전 이관
js/audio.js       효과음 · 훈련소 칩튠 · 몬스터 음성(TTS)
js/core.js        화면 전환 · 이미지 헬퍼 · HUD · --vh 계산
js/map.js         꼬불꼬불한 길(SVG) · 지점 배치 · 이동 · 펫
js/train.js       훈련소 (10칸 프레임 · 끝자리 패턴 · 포션 선택)
js/battle.js      전투 · 퍼즐방 · 보상
js/main.js        시작화면 · 갈림길 선택 · 상점 · 설정 · 엔딩
img/              그림 25장 (읽지 말 것)
songs/            song-2.mp3 ~ song-9.mp3 (선택, 없으면 내장 칩튠)
voice/            ask-2-1.mp3 / ans-2-1.mp3 (선택, 없으면 TTS)
```

## 화면 목록 (index.html의 section id)

| id | 역할 |
|---|---|
| `s-title` | 타이틀 · 이어하기 |
| `s-select` | 용사 선택 · 이름 · 난이도 |
| `s-map` | 지도 (길 위를 걸어 이동) |
| `s-train` | 훈련소 |
| `s-battle` | 몬스터 대결 |
| `s-jail` | 퍼즐방 (실패 후 약점 복습) |
| `s-result` | 전투 결과 |
| `s-choose` | 갈림길 — 다음 단 선택 |
| `s-shop` | 가방 · 상점 |
| `s-settings` | 보호자 설정 |
| `s-ending` | 엔딩 |

화면 전환은 `show('s-xxx')`. `.screen`에서 `.on` 클래스를 붙였다 뗀다.

## 게임 흐름

```
마을 → [훈련소 → 몬스터] × 8 → 드래곤 → 보물
```

- 첫 단은 2단 고정(`FIRST_TABLE`). 몬스터를 이길 때마다 **다음에 도전할 단을 직접 고른다**.
  아직 안 고른 자리는 지도에 `???`로 표시된다. 배정 정보는 `Save.s.progress.order`.
- 훈련소: ×1~×9를 10칸 프레임 그림으로 익힘 → 완료 시 포션 3종 중 하나 선택.
- 전투: ×1~×9를 랜덤 순서로 전부 한 번씩. **틀리면 즉시 재출제하지 않고 대기열 맨 뒤로 보낸다.**
  이 규칙은 학습 설계상 중요하므로 임의로 바꾸지 말 것.
- 하트 0 → 퍼즐방. 틀렸던 문제만 복습 후 **시간제한 없는** 재대결(골드 절반).
  실패한 아이에게 난이도를 더 높이지 않는다는 원칙.

## 레이아웃 규칙 (모바일)

- 화면 높이는 `100dvh`를 믿지 않는다. `core.js`의 `fixVH()`가 실제 높이를 재서 `--vh`에 넣는다.
- 내용이 긴 화면은 **[스크롤 영역] + [고정 버튼단]** 구조로 만든다.
  훈련소가 그 예시: `#tr-scroll`(스크롤) + `#tr-foot`(하단 고정).
  버튼이 화면 끝에 걸려 누르기 어려워지는 것을 막기 위함.

## 자주 고치는 값 — js/data.js

- `DIFF` : 난이도별 제한시간 · 하트 · 골드 배율
- `REWARD` : 몬스터당 골드
- `SHOP` : 상점 물건과 가격
- `PET.stages` : 펫 성장에 필요한 정답 수
- `TABLE_TIP` : 단별 추천 순서(`lv`)와 설명

## 커밋 규칙

- **커밋은 한 번에 하나씩.** 연달아 push하면 GitHub Pages 배포가 취소된다.
- push 후 배포 결과를 확인하고 보고할 것.
- 수정 후에는 실제로 브라우저에서 열어 콘솔 오류가 없는지 확인할 것.
